//! Demo path module (moved under `gfx::demo`).
//! Compiled only with `--features vox_onepath_demo`.

// Include the original demo implementation to avoid behavior changes.
include!("../vox_onepath.rs");
