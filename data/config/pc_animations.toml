# Exact animation clip names for the PC rig
# These must match keys in the merged UBC + AnimationLibrary clip set.

idle = "Idle_Loop"
walk = "Walk_Loop"
sprint = "Sprint_Loop"
strafe = "Walk_Loop" # strafe uses walk cadence for lateral motion
cast = "Spell_Simple_Idle_Loop" # back-compat; phase-specific overrides below

# Cast phase clips
cast_enter = "Spell_Simple_Enter"
cast_loop  = "Spell_Simple_Idle_Loop"
cast_shoot = "Spell_Simple_Shoot"
cast_exit  = "Spell_Simple_Exit"

# Jump state clips (from assets/anims/universal/AnimationLibrary.glb)
jump_start = "Jump_Start"
jump_loop = "Jump_Loop"
jump_land = "Jump_Land"
