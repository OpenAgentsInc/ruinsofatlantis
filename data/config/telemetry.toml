# Telemetry configuration for local development.
# Server reads this at startup; env vars override.

log_level = "info,ruinsofatlantis=info"
json_logs = true
# Uncomment to expose Prometheus metrics at this address
# metrics_addr = "127.0.0.1:9100"
# OTLP tracing endpoint (Jaeger/Tempo via OpenTelemetry Collector)
# otlp_endpoint = "http://localhost:4317"
trace_sample = 0.0
enable_client = true

